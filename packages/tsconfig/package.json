{
  "name": "tsconfig",
  "version": "0.0.1",
  "description": "Shared tsconfig.json for typescript projects",
  "private": true,
  "scripts": {
    "lint": "prettier --check . --cache",
    "fmt": "prettier --write . --cache"
  }
}
