import type { Meta, StoryObj } from "@storybook/react";

import SearchLayout from "@/app/search/layout";

export default {
  title: "APP/Search/Layout",
  component: SearchLayout,
} as Meta;
type Story = StoryObj<typeof SearchLayout>;

export const Default: Story = {};
